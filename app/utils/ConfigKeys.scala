package utils

object ConfigKeys {
  val encryptionKey: String = "encryption.key"
  val awsKeyId: String = "aws.access.key.id"
  val awsAccessKey: String = "aws.secret.access.key"
  val authPasswordSalt: String = "auth.password.salt"
  val mailgunApiKey: String = "mailgun.api.key"
}
